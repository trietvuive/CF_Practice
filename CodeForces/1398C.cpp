/*#include<string>
#include<unordered_map>
#include<iostream>
using namespace std;
long long t, n, i, cnt, ans; 
string str; 
unordered_map<int, int>mp;
int main() {
	for (cin >> t; t-- and cin >> n >> str; cout << ans << '\n')
		for (mp.clear(), mp[0] = 1, cnt = ans = i = 0; i < n; ans += mp[cnt += str[i++] - 49]++);
}
*/