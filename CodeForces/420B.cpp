#include<bits/stdc++.h>
using namespace std;
using ll = long long;
using graph = vector<vector<int>>;

const int N = 1e5 + 10;
int n,m, cnt, a[N], f[N], sm;
bool b[N], v[N];

void solve() {
	int n,m; cin >> n >> m;

	for(int i = 0; i < m; ++i) {
		char c;
		int p; 
		cin >> c >> p;
	}
}

int main() {
	ios::sync_with_stdio(false), cin.tie(nullptr);
	int tc = 1;
	while(tc--) solve();
}

